/**
 * Re-export useWallet hook for convenient import from hooks directory.
 * Usage: import { useWallet } from '@/hooks/useWallet'
 */
export { useWallet } from '../components/wallet/WalletProvider'
